/**
 *   <<<    Form 内元素变更自动ajax 提交,包含返回动画和验证结果      >>>
 *
 * Created by Great sToNe.
 *
 * Date: 13-4-7
 * Time: 下午3:34
 * EMAIL: shi35dong@gmail.com
 *
 */

(function ($) {
    $.fn.ajaxSub = function (options) {
        var defaults = {
            subAction:"blur",
            subUrl:""
        };
        //加载自定义参数
        var opt = $.extend(defaults, options);

        var actions = {
            blur: function(){

            }
        };
        var events = {
            callback:function () {

            }
        }

    }
})(jQuery);

$("input[type=text]").ajaxSub({subAction:"click"});